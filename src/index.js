import React from 'react';
import ReactDOM from 'react-dom/client';
import KeywordAnalyzerApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <KeywordAnalyzerApp />
  </React.StrictMode>
);
